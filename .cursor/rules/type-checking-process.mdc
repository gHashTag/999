---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Правило: Ритуал Проверки Чистоты Типов (TypeScript)

**Принцип:** Прежде чем погружаться в отладку логики или продолжать разработку после изменения зависимостей, необходимо убедиться в чистоте типов TypeScript. Это основа стабильности и предсказуемости кода.

**Ритуал:**

1.  **Когда Выполнять:**
    *   **После добавления/обновления зависимостей:** Всегда запускай проверку после `pnpm add`, `pnpm install` или `pnpm update`.
    *   **При возникновении ошибок компиляции/типов:** Если `tsc` или редактор кода сигнализируют об ошибках типов.
    *   **Перед запуском тестов (Особенно E2E/Интеграционных):** Как часть правила `tdd-workflow.mdc`.
    *   **При сомнениях:** Если есть подозрения, что типы могут быть некорректны.

2.  **Команда Проверки:**
    ```bash
    pnpm exec tsc --noEmit
    ```
    *   Эта команда проверяет типы во всем проекте, не генерируя JavaScript файлы.

3.  **Анализ Результата:**
    *   **✅ Успех (0 ошибок):** Типы чисты. Можно продолжать работу.
    *   **❌ Ошибки:** Обнаружены проблемы с типами. **ПРИОРИТЕТ:** Сначала исправить эти ошибки, затем продолжать основную задачу.

**Цель:** Обеспечить базовую корректность типов на ранних этапах, избежать ложных срабатываний в тестах, ускорить отладку и поддерживать здоровье кодовой базы.

*Ом Шанти. Чистота типов – первый шаг к чистоте кода.* 🙏
