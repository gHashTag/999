---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Правило: Великая Цель и Текущий Этап (Детализированный План)

**⚠️ КРИТИЧЕСКИ ВАЖНО ДЛЯ СТАБИЛЬНОСТИ ТЕСТОВ:**
*   **Ручной Запуск Сервисов:** Перед запуском тестов (особенно E2E) **необходимо** вручную запустить фоновые сервисы в отдельных терминалах: `bun run build:watch`, `bun run dev:serve`, `bun run dev:start` (см. @`README.mdc`).
*   **Упрощенное Мокирование (`@inngest/test`):** Использовать `t.execute` с опцией `steps` для мокирования шагов `step.run(...)`, а не `mock`. Это более стабильный подход для интеграционного и E2E тестирования с Inngest.
*   **Актуальные Зависимости:** Следить за версиями `@inngest/test`, `@inngest/agent-kit` и `vitest`, так как их API (особенно для мокирования) может меняться.

**Принцип:** Подобно реке, неуклонно стремящейся к океану, каждое наше действие должно быть шагом к Великой Цели. Это правило содержит высокоуровневое описание текущей задачи для Агента-Руководителя (TeamLead). Детали реализации и инструкции для конкретных агентов находятся в @`README.md` и файлах `AGENT_*.mdc`.

**Великая Цель (Дхарма Проекта):** 🌟
Создание армии автономных агентов (НейроКодеров), способных к самообучению, самоисправлению и командной работе для решения сложных задач разработки **локально и автономно**, используя гибкие LLM-провайдеры.

# 🕉️ Правило: Обязательное Общение на Русском Языке

**Принцип:** Для обеспечения наилучшего взаимопонимания и культурной гармонии, вся коммуникация с Гуру и другими участниками должна осуществляться исключительно на русском языке.

**Обязательные Действия:**
1. **Все текстовые Ответы** должны быть на русском языке, независимо от языка запроса.
2. **Все Логи и Комментарии** должны составляться на русском языке.
3. **Объяснения и Рекомендации** для Гуру должны быть на русском языке.
4. В случае необходимости использования технических терминов без прямых русских аналогов, они могут быть сохранены на английском языке, но должны быть встроены в русскоязычный контекст.

**Цель:** Обеспечить максимальную ясность коммуникации и культурную согласованность в проекте.

*Ом Шанти. Язык - мост между умами, а на родном языке мост крепче.* 🙏

---

# 🎯 **Текущий Главный Приоритет: Демонстрация Рабочего TDD-Цикла (v0.1)**

**🧘‍♂️ Цель:** Увидеть, как агенты взаимодействуют для выполнения **минимального TDD-цикла**: постановка задачи (TeamLead) -> **генерация кода** (Coder) -> проверка результата. Из-за известных проблем с `network.run()`, мы сфокусируемся на **тестировании оркестрации на уровне Inngest-функции**, мокируя шаги `step.run('run-agent-network', ...)`.

**⚠️ Ограничения:** Этот подход **не** тестирует внутреннюю логику маршрутизатора AgentKit (`network.run`), а только корректность вызова шагов и обработки их (мокированных) результатов в Inngest-функции.

**🛣️ Roadmap:**

**Фаза 1: TeamLead -> Coder (с Мокированием Сети)**

1.  ➡️ **Обновить `current_task.mdc`** (Текущий шаг).
2.  ✏️ **Адаптировать/Создать Тест Inngest-Функции:**
    *   Рассмотреть `src/__tests__/agents/codingAgent.test.ts` и тест `should generate code using InngestTestEngine`.
    *   Модифицировать его (или создать новый `tddCycle.test.ts`) для имитации более полного цикла:
        *   **Шаг 1 (Имитация TeamLead):** Мокировать первый вызов `step.run('run-agent-network', ...)` так, чтобы он возвращал состояние `NEEDS_CODE` с требованиями и кодом падающего теста (например, как сейчас в моке `getCurrentState`).
        *   **Шаг 2 (Вызов Coder):** Функция Inngest должна снова вызвать `step.run('run-agent-network', ...)`.
        *   **Шаг 3 (Имитация Coder):** Мокировать *этот* второй вызов `step.run('run-agent-network', ...)` так, чтобы он возвращал состояние `NEEDS_TYPE_CHECK` (или `NEEDS_IMPLEMENTATION_CRITIQUE`) и **сгенерированный код** (`implementation_code`).
3.  ✏️ **Проверка Генерации Кода (🟢 Зеленый):**
    *   Убедиться, что тест проходит.
    *   Проверить, что финальное состояние, возвращаемое из *второго* мокированного шага сети, содержит ожидаемый `implementation_code`.
4.  ✏️ **Рефакторинг (♻️):** Улучшить тест и моки.
5.  ✏️ **Документация и История:**
    *   Зафиксировать успех (или проблемы) в `SUCCESS_HISTORY.md` / `REGRESSION_PATTERNS.md`.
    *   Описать паттерн тестирования Inngest-функции с имитацией TDD-цикла через моки шагов.
6.  ✏️ **Коммит:** Зафиксировать изменения.

**Фаза 2: Добавление Критика (Имитация)**

*   ✏️ Расширить тест из Фазы 1: добавить третий мокированный шаг `step.run('run-agent-network', ...)` для имитации работы Критика (например, возвращающего статус `COMPLETED` или `NEEDS_IMPLEMENTATION_REVISION`).
*   ✏️ Проверить корректность итогового статуса.

**Фаза 3: (Будущее) Попытка Реальной Интеграции**

*   ✏️ После достижения успеха с имитацией, вернуться к исследованию блокеров `network.run()` или искать альтернативные способы интеграционного тестирования.

---

*Предыдущие этапы unit-тестирования агентов сохранены ниже для истории.*

# ✅ **Этап Завершён: Базовая Реализация и Unit-Тесты Агентов**

**🧘‍♂️ Цель:** Создать и провести базовое unit-тестирование для всех агентов (TeamLead, Critic, Coder, Tester, Tooling), проверяя их корректное создание, базовые свойства и фильтрацию инструментов.

**✅ Выполненные Шаги:**
*   Реализован и протестирован Агент-Руководитель (TeamLead).
*   Реализован и протестирован Агент-Критик (Critic), включая парсер ответа.
*   Реализован и протестирован Агент-Разработчик (Coder).
*   Реализован и протестирован Агент-Тестировщик (Tester).
*   Реализован и протестирован Агент-Инструментальщик (Tooling).
*   Проведена документация паттернов и регрессий.

**Коммиты:** `565b0f6`, `73e3458`, `a385e57`, `c489323`

# ... (История стабилизации и предыдущих этапов) ...

**Задачи:**

1.  ✅ **Адаптировать Тест:** В `src/__tests__/inngest/codingAgentHandler.inngest.test.ts` адаптирован тест `should generate code...` для имитации последовательных вызовов сети.
2.  ✅ **Проверить и Запустить:** Проверить типы (`tsc`) и запустить тест (`bun test ...`). Тест пройден.
3.  ✅ **Фиксация Успеха:** Зафиксирован достигнутый прогресс в `SUCCESS_HISTORY.md` и `current_task.mdc`, отмечен коммит.
4.  ➡️ **Следующий Шаг:** Расширить тест, добавив мокирование шага проверки типов (`step.run('run-type-check', ...)`), который будет вызван после успешного ответа Coder'а.
    *   **Подзадача 4.1:** Реализовать функцию `runTypeCheck` в `src/inngest/functions/runTypeCheck.ts`. Эта функция должна принимать `codeToCheck` и, вероятно, использовать `AGENT_Tooling` (или `run_terminal_cmd` напрямую в тесте для простоты) для выполнения `bun exec tsc --noEmit`. Она должна возвращать `{ success: boolean, errors: string | null }`.
    *   **Подзадача 4.2:** Добавить вызов `await step.run('run-type-check', ...)` в `codingAgentHandler` после получения ответа от Coder'а.
    *   **Подзадача 4.3:** В тесте `codingAgentHandler.inngest.test.ts` добавить мокирование шага `run-type-check`. Изначально мок должен вернуть `{ success: true }`.
    *   **Подзадача 4.4:** Добавить проверку в тесте, что шаг `run-type-check` был вызван с правильным кодом (`implementation_code`).
