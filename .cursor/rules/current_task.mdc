---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Правило: Великая Цель и Текущий Этап (Детализированный План)

**Принцип:** Подобно реке, неуклонно стремящейся к океану, каждое наше действие должно быть шагом к Великой Цели. Это правило содержит высокоуровневое описание текущей задачи для Агента-Руководителя (TeamLead). Детали реализации и инструкции для конкретных агентов находятся в @`README.md` и файлах `AGENT_*.mdc`.

**Великая Цель (Дхарма Проекта):** 🌟
Создание армии автономных агентов (НейроКодеров), способных к самообучению, самоисправлению и командной работе для решения сложных задач разработки **локально и автономно**.

**✅ Предыдущий Этап: Восстановление Среды Компиляции и Рефакторинг Структуры Тестов**
*   ✅ Переустановлен `@types/node`.
*   ✅ Исправлена конфигурация `typeRoots` в `tsconfig.json`.
*   ✅ Команда `pnpm exec tsc --noEmit` завершается успешно (Exit Code 0). Ошибки `TS6053`, `TS2688` устранены.
*   ✅ Тесты перенесены из `__tests__` в `src/__tests__`.
*   ✅ Пути обновлены в `tsconfig.json`, `.gitignore`, `package.json`, `vite.config.ts`.
*   ✅ Исправлены ошибки импортов и типов в `src/__tests__/agentDefinitions.test.ts` после переноса.

**Текущий Этап: 🧘‍♂️ Стабилизация Тестов**

🎯 **Приоритет №1 (Текущий):**
*   **Задача для Инструментальщика (Gemini):** Проверить типы и запустить тесты для проверки стабильности после всех рефакторингов.
*   **Контекст:** После переноса тестов, исправления конфигураций и типов, необходимо убедиться, что основной цикл сборки и тестирования работает корректно.
*   **Обоснование:** Следование правилу "Чистота Типов Перед Тестами" и ритуалу из `testing-workflow.mdc`.

**📝 История Попыток и Находки:**
*   ✅ **Исправлена ошибка сборки** `TS2304: Cannot find name 'Tool'` в `toolDefinitions.ts`.
*   🔴 **Запуск `build-test.sh`:** Выявил ошибки тестов (Блокер 🪵: E2E логи, MSW, Unit тесты).
*   ✅ **Логгер (`logger.ts`) модифицирован (первый раз).**
*   ✅ **Создано правило** @`testing-workflow.mdc` для документирования ритуала.
*   ✅ **Исправлена ошибка `__dirname`** в `logger.ts`.
*   🔴 **Запуск `build-test.sh` (Шаг 3 Ритуала):** Выявил 4 проваленных теста из-за несоответствия логов и таймаутов.
*   ✅ **Очистка `node_modules` и `pnpm install`:** Устранила ошибку `TypeError: TestWorkflow is not a constructor`.
*   🔴 **Повторный Запуск `build-test.sh`:** Снова показал 4 проваленных теста, указывая на проблемы с асинхронностью/логированием.
*   ❌ **Неудачное Изменение `src/inngest/index.ts`:** Попытка добавить лог привела к удалению кода обработчика.
*   ✅ **Удален дубликат** `/Users/playra/999/current_task.mdc`.
*   ✅ **Восстановлена Компиляция:** Переустановка `@types/node` и исправление `typeRoots` в `tsconfig.json` устранили ошибки `TS6053`, `TS2688`. `tsc --noEmit` проходит.
*   ✅ **Выполнен Рефакторинг Структуры Тестов:** Тесты перенесены в `src/__tests__`, конфигурации обновлены (`tsconfig.json`, `.gitignore`, `package.json`, `vite.config.ts`), ошибки типов/путей в тестах исправлены.
*   ✅ **Проверка Логирования:** Установлено, что `src/inngest/index.ts` **уже** использует контекстный логгер Inngest. Рефакторинг не требуется.

**🎯 Следующий Шаг:**
1.  Запустить `pnpm exec tsc --noEmit` для финальной проверки типов после всех изменений.
2.  Если компиляция успешна, запустить **Шаг 2** ритуала из @`testing-workflow.mdc`: `bash scripts/build-test.sh | cat` для проверки тестов.

**💡 Идея на Будущее:**
*   Создать Тест-Страж Окружения (проверяющий env, базовую инициализацию).
*   Добавить проверку `tsc --noEmit` в начало `scripts/build-test.sh`.
*   Переименовать `agentkit-state-management.mdc` в `inngest.mdc` и собирать там полезную информацию.
*   Исследовать причину проблем с разрешением алиасов `@/` в редакторе.

**📌 НАПОМИНАНИЕ:** После каждого значительного шага **ОБЯЗАТЕЛЬНО** обновлять этот файл (`.cursor/rules/current_task.mdc`), добавляя информацию в секцию **📝 История Попыток и Находки** и корректируя **Текущий Этап** и **Приоритет №1**.

---

**Приостановленный Этап: ⚙️ Интеграция Инструкций и Запуск Первого Цикла**

*   ⏸️ **Запуск Тестового TDD Цикла:** Инициировать первый полный цикл с простой задачей (например, `add(a, b)`). **(Ждет завершения этапа отладки)**

---

**Отложенные Этапы:**
*   ⏸️ **Завершение Улучшения TDD Оркестровки (с E2B):** Исправление ошибок типов/линтера, верификация потока.
*   ⏸️ **PoC - Управление `open-codex` через Агентов.**

**Цель Правила:** Служить **путеводной звездой** для текущего этапа разработки, определяя приоритеты и следующие шаги.

*Ом Шанти. Познай Кшетру, прежде чем действовать.* 🙏
