---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Правило: Великая Цель и Текущий Этап (Детализированный План)

**Принцип:** Подобно реке, неуклонно стремящейся к океану, каждое наше действие должно быть шагом к Великой Цели. Это правило служит детализированным планом текущего этапа, отражая прогресс с помощью статусов ✅, ❌, ⏳. Оно обновляется после каждого значимого шага.

**Великая Цель (Дхарма Проекта):** 🌟
Создание армии автономных агентов (НейроКодеров), способных:
*   Самостоятельно изучать, понимать и обновлять кодовую базу.
*   Самосовершенствоваться, исправлять ошибки и улучшать качество кода.
*   Работать параллельно над различными задачами.
*   Обмениваться состоянием и знаниями, действуя как единый организм.
*   Формировать команды с различными ролями (лидеры, мотиваторы, кодеры, тестеры, аналитики и т.д.).
*   Адаптироваться к любым задачам, при необходимости создавая новые роли/агентов.

**Текущий Этап: Прототип НейроКодера (Coding Agent) - ФОКУС НА СБОРКЕ/ТЕСТАХ**

🎯 **Цель Этапа:** **Приоритет №1:** Исправить систему сборки (`pnpm run build`) и тестирования (`pnpm run test:node-example`) для `[examples/node/index.ts](mdc:examples/node/index.ts)`, чтобы обеспечить надежный фундамент для дальнейшей разработки и применения TDD.

**Подзадачи Текущего Этапа (со статусами):**

1.  **🚨 Исправление Сборки и Тестов (ВЫСОКИЙ ПРИОРИТЕТ):**
    *   ❌ **Сборка `pnpm run build`:** Не работает (ошибка `TS2307 Cannot find module 'inngest'`).
    *   ❌ **Тесты `pnpm run test:node-example`:** Не работают (та же ошибка `TS2307`, Jest не находит workspace-модули).
    *   ⏳ **Исследование:**
        *   (Испробовано: `tsconfig` paths, `moduleResolution`, `composite`/`references`, `jest` `moduleNameMapper`, упрощение `jest.config`, проверка сборки зависимостей, удаление `exports`).
        *   ⏳ **Следующие шаги:** Глубже изучить конфигурацию монорепо, возможно, создать минимальный пример для изоляции проблемы, искать похожие issues в GitHub.
    *   ✅ **Обходной Путь (НЕ ИСПОЛЬЗОВАТЬ ПОКА НЕ ИСПРАВЛЕНО):** `npx inngest-cli dev` позволяет запускать, **но к пользователю с просьбой отправки событий обращаться НЕЛЬЗЯ**, пока Приоритет №1 не выполнен.
2.  **Настройка и Запуск (Базовый - необходимо для тестов):**
    *   ✅ Настроено окружение (`.env`, зависимости).
    *   ✅ Агент запускается через `npx inngest-cli dev` (но не используем для тестирования).
    *   ✅ Агент принимает базовые задачи (`coding-agent/run`) (проверено ранее).
3.  **Базовая Генерация Кода (проверено ранее):**
    *   ✅ Агент может сгенерировать простой скрипт (`get_request.js`).
    *   ✅ Агент сохраняет результат в артефакт (`artifact-...tar.gz`).
    *   ✅ Содержимое артефакта соответствует задаче.
4.  **Тестирование Инструментов (TDD Подход - ЗАБЛОКИРОВАНО):**
    *   (Тесты не запускаются из-за ошибки TS2307)
    *   ⏳ **Инструмент `terminal`:** ...
    *   ⏳ **Инструмент `createOrUpdateFiles`:** ...
    *   ⏳ **Инструмент `readFiles`:** ...
    *   ⏳ **Инструмент `runCode`:** ...
5.  **Завершение Цикла и Результат (ЗАБЛОКИРОВАНО):**
    *   ⏳ **Возврат `<task_summary>`:** ...
    *   ⏳ **Полный Цикл:** ...

**Цель Правила:** Служить актуальным, детализированным планом текущего этапа, **подчеркивая приоритет исправления сборки/тестов**, отслеживать прогресс с помощью статусов ✅/❌/⏳, напоминать о Великой Цели и применении TDD, обеспечивая прозрачность и фокус.

*Ом Шанти. Прочный фундамент - основа высокого храма.* 🙏
