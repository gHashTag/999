---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Правило: Великая Цель и Текущий Этап (Детализированный План)

**Принцип:** Подобно реке, неуклонно стремящейся к океану, каждое наше действие должно быть шагом к Великой Цели. Это правило служит детализированным планом текущего этапа, отражая прогресс с помощью статусов ✅, ❌, ⏳. Оно обновляется после каждого значимого шага.

**Великая Цель (Дхарма Проекта):** 🌟
Создание армии автономных агентов (НейроКодеров), способных к самообучению, самоисправлению и командной работе для решения сложных задач разработки.

**Текущий Этап: Прототип НейроКодера - Базовая Функциональность и TDD Оркестровка**

🎯 **Приоритет №1 (Разрешен):** Стабильная сборка и запуск (на порту 8484).
🎯 **Приоритет №2 (Текущий):** Реализация и тестирование TDD-потока с 3 агентами.

**Подзадачи Текущего Этапа (со статусами):**

1.  **🏗️ Сборка и Запуск:**
    *   ✅ **Сборка `pnpm run build`:** Проходит успешно после исправлений ошибок компиляции.
    *   ✅ **Запуск (`inngest-cli dev` + `node dist/index.js` раздельно):** Работает стабильно при явном указании URL (`-u`) для `inngest-cli`. Проблема повторов (`HANDLER START` много раз) сохраняется (Issue #146), но игнорируется.
    *   ✅ Настроено окружение (`.env`, зависимости).
    *   ✅ Агент принимает базовые задачи (`coding-agent/run`).
2.  **🧪 Тестирование Инструментов (Базовое):**
    *   ✅ **`terminal`:** Протестирован ранее (хотя логи были неполными).
    *   ✅ **`createOrUpdateFiles`:** Косвенно протестирован ранее.
    *   ⏳ Остальные (`readFiles`, `runCode`) будут тестироваться в рамках TDD-потока.
3.  **🤖 TDD Оркестровка (3 Агента - ТЕКУЩИЙ ФОКУС):**
    *   ✅ **Рефакторинг Сети (`src/network.ts`):** Добавлены 3 агента (`tester`, `coder`, `critic`), реализован роутер на основе статусов (`NEEDS_TEST`, `NEEDS_CODE`, `NEEDS_CRITIQUE`, `COMPLETED`).
    *   ✅ **Рефакторинг Точки Входа (`src/index.ts`):** Создаются 3 агента, инициализируется сеть, устанавливается начальный статус `NEEDS_TEST` и `sandboxId` в `state.kv`.
    *   ✅ **Тестирование Потока (Логи):** Запущено тестовое событие "add function". Логи показывают успешное прохождение задачи через всех трех агентов (Tester -> Coder -> Critic) и завершение цикла роутером.
    *   ❌ **Проверка Результата (Файлы):** Не проверено, были ли реально созданы файлы `test.js` и `implementation.js` в песочнице (песочница убита).
    *   ⚠️ **Проблема: Финальное состояние:** Лог `[HANDLER] Final Network State` показывает некорректный статус (`NEEDS_TEST` вместо `COMPLETED`). Требует исследования.
    *   ⏳ **Улучшение Хуков (`onResponse`):** Текущие хуки используют плейсхолдеры для имен файлов (`test_code = "test.js"`). Нужно добавить чтение реального содержимого файлов и сохранение его в `state.kv`.
    *   ⏳ **Исследование Финального Состояния:** Понять, почему в конце `codingAgentHandler` читается неактуальное состояние.
4.  **✨ Качество Кода:**
    *   ✅ Большинство ошибок линтера исправлено.
    *   ✅ Ошибки импортов (`ToolHandlerArgs`, `AgentRunOpts`) исправлены.
    *   ⚠️ Остались предупреждения `any` в `src/network.ts` и `src/index.ts` (связано с `Agent<any>` и типом `step`).
    *   ⏳ Требуется коммит изменений после рефакторинга.
5.  **Задачи для Будущего Исследования:** 🔬
    *   Исследовать проблемы с импортами/выполнением `vite` в тестах сети (`src/index.test.ts`), которые привели к пропуску тестов.
    *   Исследовать корректные методы E2B SDK для `downloadArtifact` (вместо `kill()`, если нужно содержимое).
    *   Вернуться к проблеме множественных вызовов обработчика (Issue #146) позже.
    *   Исследовать точный тип для `step` контекста Inngest.
    *   Рефакторинг: Вынести определение инструментов из `src/index.ts`.
    *   Рефакторинг: Переделать передачу `sandboxId` инструментам через `state.kv` вместо замыкания.

**Цель Правила:** Служить актуальным, детализированным планом текущего этапа, **отслеживать прогресс** (✅/❌/⏳/⚠️), **фокусироваться на TDD-оркестровке**, напоминать о Великой Цели.

*Ом Шанти. Путь к цели лежит через понимание и исправление несовершенств.* 🙏
