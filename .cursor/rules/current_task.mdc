---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Правило: Великая Цель и Текущий Этап (Детализированный План)

**⚠️ КРИТИЧЕСКИ ВАЖНО ДЛЯ СТАБИЛЬНОСТИ ТЕСТОВ:**
*   **Ручной Запуск Сервисов:** Перед запуском тестов (особенно E2E) **необходимо** вручную запустить фоновые сервисы в отдельных терминалах: `bun run build:watch`, `bun run dev:serve`, `bun run dev:start` (см. @`README.mdc`).
*   **Упрощенное Мокирование (`@inngest/test`):** Использовать `t.execute` с опцией `steps` для мокирования шагов `step.run(...)`, а не `mock`. Это более стабильный подход для интеграционного и E2E тестирования с Inngest.
*   **Актуальные Зависимости:** Следить за версиями `@inngest/test`, `@inngest/agent-kit` и `vitest`, так как их API (особенно для мокирования) может меняться.

**Принцип:** Подобно реке, неуклонно стремящейся к океану, каждое наше действие должно быть шагом к Великой Цели. Это правило содержит высокоуровневое описание текущей задачи для Агента-Руководителя (TeamLead). Детали реализации и инструкции для конкретных агентов находятся в @`README.md` и файлах `AGENT_*.mdc`.

**Великая Цель (Дхарма Проекта):** 🌟
Создание армии автономных агентов (НейроКодеров), способных к самообучению, самоисправлению и командной работе для решения сложных задач разработки **локально и автономно**, используя гибкие LLM-провайдеры.

# 🕉️ Правило: Обязательное Общение на Русском Языке

**Принцип:** Для обеспечения наилучшего взаимопонимания и культурной гармонии, вся коммуникация с Гуру и другими участниками должна осуществляться исключительно на русском языке.

**Обязательные Действия:**
1. **Все текстовые Ответы** должны быть на русском языке, независимо от языка запроса.
2. **Все Логи и Комментарии** должны составляться на русском языке.
3. **Объяснения и Рекомендации** для Гуру должны быть на русском языке.
4. В случае необходимости использования технических терминов без прямых русских аналогов, они могут быть сохранены на английском языке, но должны быть встроены в русскоязычный контекст.

**Цель:** Обеспечить максимальную ясность коммуникации и культурную согласованность в проекте.

*Ом Шанти. Язык - мост между умами, а на родном языке мост крепче.* 🙏

---

# ✅ **Этап Восстановления: Возврат к Стабильности (Коммит a172324)**

**🧘‍♂️ Цель:** Найти стабильную точку в истории проекта, где основные тесты проходили, и использовать её как базу для дальнейшей работы.

**Выполненные Задачи:**
1.  ✅ Проанализирован `git log` для поиска потенциально стабильного коммита.
2.  ✅ Выбран коммит `a172324`.
3.  ✅ Выполнен `git stash` для сохранения текущих некоммиченных изменений.
4.  ✅ Выполнен `git checkout a172324`.
5.  ✅ Проведена проверка типов (`bun exec tsc --noEmit`), выявлены ошибки.
6.  ✅ Исправлены ошибки типов в `src/__tests__/adapters/mcpAdapter.integration.test.ts`, `src/adapters/deepseekAdapter.ts`, `src/network/routerLogic.ts`.
7.  ✅ Повторная проверка типов прошла (с одной допустимой ошибкой приведения типов).
8.  ✅ Запущены тесты `bun test src/__tests__/`: **50 тестов прошли, 5 пропущены, 0 упали.**
9.  ✅ Создана новая ветка `feat/stabilize-tests-from-a172324`.
10. ✅ Закоммичены исправления ошибок типов (`168d506`).

---

# ✅ **Этап: Анализ и Запуск Пропущенных Тестов - Завершен**

**🧘‍♂️ Цель:** Восстановить тестовое покрытие, анализируя и запуская тесты, которые были пропущены (`skip`) в коммите `a172324`.

**✅ Выполненные Задачи:**
*   ✅ **Тест `TeamLead Agent Integration...`:** Проанализирован, разблокирован путем замены адаптера, но помечен как `skip` из-за внешней проблемы **agent-kit Issue #147** (асинхронное обновление KV).
*   ✅ **Тест `Update Task State Tool ... > should log start and end messages`:** Проанализирован, разблокирован, исправлен и **успешно пройден**.
*   ✅ **Тест `Agent Definitions: Coder Agent > should generate code...`:** Проанализирован. Оставлен как `skip`, так как тело теста отсутствует и он зависит от проблемной библиотеки `@inngest/test`.
*   ✅ **Тест `MCP Adapter Integration > должен логировать ключевые события...`:** Проанализирован, разблокирован, исправлен и **успешно пройден**.
*   ✅ **Тест `TeamLead Agent Workflow (E2E...)`:** Проанализирован. Оставлен как `skip` из-за зависимости от проблемной библиотеки `@inngest/test`.

**📊 Результат:**
*   **Восстановлено тестов:** 2
*   **Остаются пропущенными (`skip`):** 3 (TeamLead Integration [AgentKit #147], Coder Gen [InngestTest], TeamLead E2E [InngestTest])
*   **Текущий статус:** 52 теста проходят, 3 пропущено.

---

# 🔄 **Текущий Этап: Обновление Зависимостей и Повторная Проверка**

**🧘‍♂️ Цель:** Попытаться решить проблемы с `@inngest/agent-kit` и `@inngest/test`, обновив зависимости до последних версий и повторно проверив заблокированные тесты.

**⚠️ Риск:** Обновление может сломать работающие тесты или внести другие несовместимости.

**📊 План Действий:**
1.  ➡️ **Обновить зависимости:** Выполнить `bun update` (или `pnpm update`, если используется pnpm).
2.  ✏️ **Проверить типы:** Запустить `bun exec tsc --noEmit`. Исправить возникшие ошибки типов.
3.  ✏️ **Запустить ВСЕ тесты:** Запустить `bun test`. Проанализировать результат. Исправить упавшие тесты, *если это не связано с известными блокерами*.
4.  ✏️ **Повторно проверить блокеры:** Попробовать запустить 3 пропущенных теста (убрав `.skip` по очереди), чтобы проверить, не исправили ли обновления проблемы в `agent-kit` и `@inngest/test`.
5.  ✏️ **Зафиксировать результат:** Обновить `current_task.mdc` и `SUCCESS_HISTORY.md`/`REGRESSION_PATTERNS.md`.

---
*История Quick Start и Фабрики Reels сохранена ниже для контекста.*

# (ПРИОСТАНОВЛЕНО) 🔄 Текущий Этап: Возврат к Основам - Quick Start AgentKit

**🧘‍♂️ Цель:** Пройти шаги из **AgentKit Quick Start Guide** ([https://agentkit.inngest.com/getting-started/quick-start](mdc:https:/agentkit.inngest.com/getting-started/quick-start)), чтобы создать и протестировать **базового агента** (например, `dbaAgent` из гайда) в нашем окружении. Это должно подтвердить работоспособность `createAgent` и `agent.run` с OpenRouter и дать нам стабильную основу.

**🚧 Предыдущие Задачи (Приостановлены):**
*   Отладка ошибки `TypeError` в `network.run` (`agent-kit`).
*   Реализация агента "Фабрика Reels".
*   Отладка `EchoAgent` (он свою задачу выполнил, подтвердив базовую работу).

**📊 План Действий (Quick Start):**
1.  ✅ **Обновить эту цель.**
2.  ✅ **Создать файл теста:** `src/__tests_focused__/quickstart/dbaAgent.test.ts`.
3.  ✅ **Определить Агента:** Внутри теста определить `dbaAgent` (из Quick Start), адаптировав под OpenRouter (`openai` адаптер, модель `qwen/qwen3-30b-a3b:free`).
4.  ✅ **Написать Unit-Тест:** Проверить базовый вызов `dbaAgent.run("...")` и получение ответа без ошибок.
5.  ✅ **Проверить Типы:** Запустить `bun exec tsc --noEmit`.
6.  ✅ **Запустить Тест:** Запустить `bun test src/__tests_focused__/quickstart/dbaAgent.test.ts --isolate`.
7.  ➡️ **Зафиксировать Результат:** Обновить `current_task.mdc` и `SUCCESS_HISTORY.md` (при успехе).
8.  ✏️ **Следующий Шаг (после успеха):** Решить, возвращаемся ли к отладке `network.run` или продолжаем усложнять тесты базовых агентов.

---
*Вся предыдущая история и roadmap сохранены ниже.*

# (ПРИОСТАНОВЛЕНО) 🕉️ Roadmap: Реализация Агента "Фабрика Reels" (v2: Supabase + Apify/MCP)

**Великая Цель:** Автоматизировать процесс поиска, отбора, генерации и управления контентом для Instagram Reels в нише эстетической медицины.

**Технологический Стек (Уточненный):**
*   **Оркестрация:** n8n / Zapier / Make / Inngest (или другая система, где будут реализованы инструменты)
*   **Парсинг:** **Apify Акторы**, запускаемые через **MCP-сервер** (требуется доступный инструмент типа `mcpApifyTool`)
*   **Хранение:** **Supabase** (требуется доступный инструмент типа `supabaseTool` или прямая интеграция с API)
*   **AI Генерация:** HeyGen API / RunwayML API / MidJourney API (или другие)
*   **Хранение Файлов:** Google Drive API
*   **Уведомления:** Telegram Bot API / Email API (SendGrid, etc.)
*   **Язык (для скриптов/интеграций):** Зависит от оркестратора и доступных инструментов/библиотек.

---

## 🚧 **Текущий Блокер и Приоритет №1:** Стабилизация Сети AgentKit

*   **Проблема:** Интеграционные тесты сети (`network.run`) **заблокированы** из-за внутренней ошибки `agent-kit` (`TypeError: Cannot read properties of undefined (reading 'request')`). Это препятствует проверке автономного взаимодействия агентов.
*   **➡️ Следующий Активный Шаг:** **Исследовать и устранить ошибку `TypeError` в `network.run` (`agent-kit`).**
    *   **Подшаг 1:** Локализовать точное место возникновения ошибки в коде `agent-kit` (если возможно) или в нашем коде, вызывающем `network.run`.
    *   **Подшаг 2:** Попробовать обновить зависимость `@inngest/agent-kit` до последней версии (если еще не сделано) и проверить, решена ли проблема.
    *   **Подшаг 3:** Поискать известные проблемы (issues) в репозитории AgentKit или Inngest, связанные с этой ошибкой.
    *   **Подшаг 4:** Если проблема не решена обновлением или не является известной, подготовить минимальный воспроизводимый пример и сообщить об ошибке разработчикам `agent-kit`.
    *   **Подшаг 5:** Рассмотреть временные обходные пути (если возможно), не нарушая основную архитектуру.

---

## 📊 Декомпозиция Задачи "Фабрика Reels" (Задачи, Зависящие от Решения Блокера)

### Фаза 1: MVP - Настройка Инфраструктуры, Парсинг и Хранение (🗓️) - *Ожидает решения блокера AgentKit*
*   **1.1. ✏️ Настройка Supabase:**
    *   Создать проект Supabase.
    *   Определить и создать структуру таблицы (Колонки: `published_at`, `source_account_or_hashtag`, `reel_url`, `views_count`, `caption`, `ai_generation` (boolean, default: null), `final_content_url` (text, nullable), `status` (text, e.g., 'new', 'processed', 'error'), `created_at`, `updated_at`).
    *   Получить URL проекта, `anon` ключ и `service_role` ключ.
*   **1.2. ✏️ Настройка Apify Актора:**
    *   Выбрать или настроить Apify актор для парсинга Instagram Reels.
    *   Убедиться, что актор может принимать список аккаунтов/хэштегов и возвращает необходимые поля.
    *   Получить ID актора Apify.
*   **1.3. ✏️ Реализация Инструментов:**
    *   Создать и интегрировать инструмент `mcpApifyTool` (вызов Apify через MCP).
    *   Создать и интегрировать инструмент `supabaseTool` (взаимодействие с Supabase).
*   **1.4. ✏️ Реализация Логики Парсинга и Фильтрации:**
    *   Настроить/реализовать вызов `mcpApifyTool`.
    *   Реализовать фильтрацию результатов (дата ≤ 14 дней, просмотры ≥ 50k).
    *   Обработать ошибки вызова Apify или парсинга.
*   **1.5. ✏️ Интеграция с Supabase:**
    *   Настроить/реализовать вызов `supabaseTool` для вставки отфильтрованных данных.
    *   Обеспечить уникальность записей.
*   **1.6. ✅ Тестирование MVP (Unit и Интеграция):**
    *   Написать unit-тесты для новых инструментов.
    *   Написать интеграционные тесты для потока парсинга-фильтрации-сохранения (требует работающей сети AgentKit).

### Фаза 2: Логика Анализа (AI vs Manual) (🗓️) - *Ожидает Фазу 1*
*   ... (шаги без изменений) ...

### Фаза 3: Интеграция Генерации Контента (AI) (🗓️) - *Ожидает Фазу 2*
*   ... (шаги без изменений) ...

### Фаза 4: Интеграция Уведомлений (🗓️) - *Ожидает Фазу 3*
*   ... (шаги без изменений) ...

### Фаза 5: Автоматизация и Оркестрация (🗓️) - *Ожидает Фазу 4*
*   ... (шаги без изменений) ...

### Фаза 6: Мониторинг и Улучшение (🚀) - *Постоянно*
*   ... (шаги без изменений) ...

---

*Статус: Стабилизация завершена (52/55 тестов).* *Известные блокеры: AgentKit#147, @inngest/test.*
*Следующий шаг: Обновление зависимостей.*

**✅ Этап Завершён: Стабилизация Тестов и Слияние в `main`.**

**📊 Итоговый Статус Тестов (на момент `main` @ `59d3deb`):**
*   **Проходят:** 52
*   **Пропущено:** 3 (1 Coder Inngest [@inngest/test], 1 TeamLead Network [agent-kit], 1 E2E [@inngest/test/agent-kit])
*   **Падают:** 0

**➡️ Следующий Активный Шаг:**
*   **Определение Приоритетов:** Обсудить с Гуру и определить следующий фокус:
    1.  **Новая Функциональность:** Начать работу над новой фичей (например, реализация недостающих агентов, подключение к реальному MCP, разработка UI). Требуется создание новой ветки `feat/...` от `main`.
    2.  **Исправление Пропущенных Тестов:** Попробовать найти обходные пути для проблем с `@inngest/test` или `agent-kit`, чтобы разблокировать оставшиеся 3 теста. Требуется создание новой ветки `fix/...` от `main`.
    3.  **Рефакторинг/Улучшения:** Провести рефакторинг существующего кода (например, улучшить обработку ошибок, типизацию, документацию). Требуется создание новой ветки `refactor/...` от `main`.
*   **Обновление Задачи:** После выбора Гуру обновить этот файл (`current_task.mdc`) с детальным планом для новой задачи.
*   **Текущий статус:** Ожидание указаний от Гуру.

**Текущий Этап: ⚙️ Углубление Логики: Агент-Руководитель (TeamLead) - Шаг 1 (Unit-Тесты)**

**Цель:** Реализовать и протестировать основную логику Агента-Руководителя - декомпозицию задачи на требования. Сосредоточиться на проверке системного промпта и, возможно, на unit-тестировании функции-парсера ответа LLM (если она понадобится).

**Важно:** Мы помним о проблемах с мокированием `agent.run()` для проверки сквозного вызова LLM в unit-тестах. Вместо этого будем тестировать компоненты: системный промпт и парсинг ответа.

**📊 План Действий:**

1.  ➡️ **Обновить `current_task.mdc`** (Текущий шаг).
2.  ✏️ **Проверить/Написать Тест Системного Промпта (🔴 -> 🟢):**
    *   Убедиться в наличии/написать тест в `src/__tests__/agents/teamLeadAgent.unit.test.ts`, который проверяет, что системный промпт (`agent.system`) Агента-Руководителя содержит четкие инструкции по декомпозиции задачи и формат вывода (нумерованный/маркированный список требований).
    *   Запустить тест, добиться прохождения (путем корректировки инструкций, передаваемых в `createTeamLeadAgent`).
3.  ✏️ **Реализовать Функцию-Парсер Требований (если нужно):**
    *   **Анализ:** Решить, должен ли Руководитель сам парсить строку ответа LLM в массив требований, или этим займется Оркестратор/следующий агент.
    *   **Если Парсер Нужен:**
        *   Добавить функцию `parseTeamLeadResponse(llmOutput: string): string[] | null` в `src/agents/teamlead/logic/createTeamLeadAgent.ts`.
        *   Написать Unit-Тесты для парсера (🔴 -> 🟢) в `teamLeadAgent.unit.test.ts` (валидный список, невалидный, пустой ответ).
        *   Рефакторинг парсера (♻️).
    *   **Если Парсер НЕ Нужен:** Пропустить этот шаг.
    *   *(Предварительное решение: Скорее всего, Руководитель должен возвращать сырую строку, а парсинг - задача Оркестратора или Тестировщика, чтобы не усложнять агента. Оставим этот шаг пока пропущенным).* 
4.  ✏️ **Рефакторинг (♻️):** Улучшить код агента (`createTeamLeadAgent.ts`) и тестов (`teamLeadAgent.unit.test.ts`), добавить JSDoc, вынести константы.
5.  ✏️ **Документация и История:**
    *   Зафиксировать удачные паттерны системных промптов или тестирования в `SUCCESS_HISTORY.md`.
    *   Записать возникшие проблемы или неудачные подходы в `REGRESSION_PATTERNS.md`.
6.  ✏️ **Коммит:** Зафиксировать изменения.
7.  ✏️ **Следующий Шаг:** Переход к углублению логики следующего агента (Критик?).

---

*Предыдущая история и roadmap сохранены ниже без изменений.*
