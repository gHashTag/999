---
description: 
globs: 
alwaysApply: true
---
# 📜 Руководство по Скриптам Проекта

Эта директория содержит полезные shell-скрипты для автоматизации общих задач разработки и развертывания.

## Доступные Скрипты

*   **[`setup-dev.sh`](mdc:scripts/setup-dev.sh):**
    *   **Назначение:** Устанавливает все зависимости проекта (`pnpm install`) и настраивает Git-хуки (`husky`).
    *   **Запуск:** `bash scripts/setup-dev.sh`
    *   **Когда использовать:** После клонирования репозитория или при обновлении зависимостей.

*   **[`quality-check.sh`](mdc:scripts/quality-check.sh):**
    *   **Назначение:** Запускает проверки форматирования кода (`prettier --check`) и линтинга (`eslint .`). Помогает убедиться в чистоте кода перед коммитом.
    *   **Запуск:** `bash scripts/quality-check.sh`
    *   **Когда использовать:** Перед созданием коммита, чтобы убедиться в отсутствии проблем с качеством кода.

*   **[`build-test.sh`](mdc:scripts/build-test.sh):**
    *   **Назначение:** Выполняет сборку проекта (`pnpm run build`) и запускает все тесты (`pnpm run test`).
    *   **Запуск:** `bash scripts/build-test.sh`
    *   **Когда использовать:** Для проверки, что проект успешно собирается и все тесты проходят.

*   **[`deploy.sh`](mdc:scripts/deploy.sh):**
    *   **Назначение:** Выполняет развертывание приложения на удаленный сервер (`999-multibots-u14194.vm.elestio.app`) через SSH. Пересобирает и перезапускает docker-compose сервисы.
    *   **Запуск:** `bash scripts/deploy.sh` (потребуется подтверждение)
    *   **Когда использовать:** Для развертывания новой версии приложения на сервере.
    *   **Примечание:** Убедитесь, что ваш SSH-ключ (`~/.ssh/id_rsa`) настроен для доступа к серверу.

## Другие команды

Для других стандартных задач (запуск в режиме разработки, отдельные шаги сборки/тестирования/линта) смотрите секцию `scripts` в файле [`package.json`](mdc:package.json).
