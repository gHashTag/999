---
description: 
globs: 
alwaysApply: true
---
# Руководство по разработке через тестирование (TDD) - НЕУКОСНИТЕЛЬНО!

**Принцип:** Мы **строго** придерживаемся дисциплины TDD. Отступление от этого пути ведет к хаосу и ошибкам. **Каждый цикл работы над подзадачей из `current_task.mdc` должен начинаться с шага 1.**

Чтобы обеспечить качество кода, удобство обслуживания и сократить регрессии, мы **обязаны** следовать принципам разработки через тестирование:

1.  ⚙️ **ЗАПУСТИ РЕЛЕВАНТНЫЙ ТЕСТ:** Определи и запусти тест (unit, интеграционный, e2e), который проверяет текущую подзадачу из `current_task.mdc`.
    *   **Если теста нет:** Переходи к Шагу 2.
    *   **Если тест падает (🔴 Красный):** Переходи к Шагу 3.
    *   **Если тест проходит (✅ Зеленый):** Переходи к Шагу 4.
2.  🔴 **СНАЧАЛА ТЕСТ!** Если для подзадачи нет теста, **НЕОБХОДИМО** написать автоматизированный тест, который описывает желаемую функциональность. Этот тест **ДОЛЖЕН ПРОВАЛИТЬСЯ**, потому что функциональность еще не существует. **НИКАКОГО КОДА РЕАЛИЗАЦИИ ДО ПРОВАЛЬНОГО ТЕСТА!** После написания провального теста, переходи к Шагу 3.
3.  ✅ **МИНИМУМ КОДА ДЛЯ ПРОХОЖДЕНИЯ:** Напишите **только тот** код реализации, который необходим для прохождения только что написанного (Шаг 2) или существующего провального (Шаг 1) теста. Ни строчкой больше. Простота – путь к истине. После этого **ВЕРНИСЬ К ШАГУ 1**, чтобы убедиться, что тест теперь зеленый.
4.  ♻️ **РЕФАКТОРИНГ:** **Только после** того, как тест пройден (Шаг 1 -> Зеленый), выполните рефакторинг кода реализации (и, возможно, тестового кода), чтобы повысить ясность, удалить дублирование и обеспечить соблюдение принципов проектирования. После рефакторинга **ВЕРНИСЬ К ШАГУ 1**, чтобы убедиться, что все тесты по-прежнему проходят.
5.   **🙏 Взаимодействие с Гуру:** Когда требуется твое решение или указание (согласно правилу [`autonomy`](mdc:.cursor/rules/autonomy.mdc)), я буду предлагать **нумерованные варианты действий (1, 2, 3...).** Пожалуйста, отвечай цифрой, соответствующей выбранному пути, для ускорения нашего взаимодействия.
   
   # 🕉️ Правило: Гранулярное Тестирование (Декомпозиция по Шагам)

**Принцип:** Подобно тому, как река прокладывает русло камень за камнем, сложные задачи разработки решаются шаг за шагом, и каждый шаг должен быть покрыт отдельным, сфокусированным тестом.

1.  **Декомпозируй Задачу:** Прежде чем писать код или тесты, разбей текущую подзадачу (из `current_task.mdc` или логического потока) на **минимальные, последовательные шаги**.
2.  **Один Шаг = Один Тест (`it`):** Для **каждого** такого шага напиши отдельный, сфокусированный тест (чаще всего это блок `it(...)` внутри `describe(...)`).
3.  **Тестируй Конкретный Результат:** Каждый тест должен проверять **один конкретный аспект** или результат выполнения соответствующего шага (например, "сервер запущен", "событие отправлено", "запись в логе появилась", "состояние изменилось").
4.  **Последовательность (Если Важно):** Если шаги зависят друг от друга (особенно в интеграционных/E2E тестах), используй `describe.sequential` или другие механизмы Vitest/Jest для обеспечения их последовательного выполнения.
5.  **Фокус на Первом Падении:** При запуске тестов, **сосредоточься на исправлении самого первого упавшего теста (🔴)** в последовательности. Не переходи к следующим, пока первый не станет зеленым (✅).
6.  **Структура E2E/Интеграционных Тестов:**
    *   Минимизируй логику в `beforeAll` / `afterAll`. В идеале, там только базовая подготовка/очистка среды, которая сама по себе не является проверяемым шагом бизнес-логики.
    *   Запуск зависимых сервисов (базы данных, внешние API, другие компоненты системы) **должен быть частью отдельных `it` блоков** в начале теста, чтобы явно проверить их готовность.
    *   Каждый `it` блок должен явно описывать проверяемый шаг (например, `it('should start the application server')`, `it('should send the event successfully')`, `it('should update the state to NEEDS_CODE')`).

**Цель:** Сделать тесты более читаемыми, поддерживаемыми, точно локализующими ошибки и строго следующими принципам TDD при решении сложных задач.

*Ом Шанти. Малое усилие, приложенное в верном месте, сдвигает горы.* 🙏
  
# 🕉️ Правило: Чистота Типов Перед Тестами

**Принцип:** Нельзя войти в храм с грязными ногами, нельзя запускать тесты с ошибками типов. Любой запуск тестов (`vitest run`, `pnpm test`, `build-test.sh` и т.д.) должен предваряться успешной проверкой типов TypeScript.

1.  **Обязательная Проверка:** Перед выполнением команды запуска тестов (особенно E2E или интеграционных), **НЕОБХОДИМО** убедиться в отсутствии ошибок компиляции TypeScript.
2.  **Способы Проверки:**
    *   **Явная команда:** `pnpm exec tsc --noEmit` (проверяет типы без генерации JS файлов).
    *   **Неявно через сборку:** Успешное выполнение `pnpm run build` также гарантирует отсутствие ошибок типов.
3.  **Исправление Прежде Всего:** Если проверка типов выявила ошибки (🔴), **СНАЧАЛА** нужно исправить эти ошибки, и **ТОЛЬКО ПОТОМ** запускать тесты.

**Цель:** Обеспечить базовую корректность кода перед его тестированием, избежать траты времени на запуск заведомо неработающих тестов и ложных срабатываний из-за проблем с типами.

**Наблюдение за Выводом:**
*   **Автономный Анализ (Предпочтительно):** Как описано выше, запускай процессы в фоне (`&` или `is_background=True`) и анализируй логи/артефакты постфактум.
*   **Интерактивное Наблюдение (При Необходимости):** Если для диагностики **крайне необходимо** наблюдать за выводом процесса в реальном времени, запускай его **в основном потоке** (foreground) – **без `&` в конце команды в твоем терминале, или `is_background=False` при использовании `run_terminal_cmd` мной**. Это позволит видеть stdout и stderr непосредственно во время выполнения. Используй этот метод с осторожностью, чтобы не блокировать терминал надолго.


*Ом Шанти. Чистота кода начинается с чистоты типов.* 🙏

**Основные преимущества (Напоминание!):**

*   **Уверенность:** Тесты действуют как страховочная сетка, придавая уверенность при рефакторинге или добавлении новых функций.
*   **Проектирование:** Написание тестов в первую очередь способствует лучшему, более тестируемому проектированию кода.
*   **Документация:** Тесты служат исполняемой документацией для того, как должен работать код.
*   **Сокращенная отладка:** Обнаруживает ошибки на **САМОМ РАННЕМ** этапе.

**НЕУКОСНИТЕЛЬНОЕ СОБЛЮДЕНИЕ TDD – ЗАЛОГ НАШЕГО СПОКОЙСТВИЯ И ЧИСТОТЫ КОДА.** 🙏 Помните о страданиях, вызванных отступлением от этого пути!