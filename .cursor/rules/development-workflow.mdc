---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Правило: Стабильный Запуск Среды Разработки

**Принцип:** Запускай компоненты среды разработки (компилятор, Inngest, сервер приложения) раздельно и **только если они еще не запущены**, чтобы избежать ненужных перезапусков и конфликтов портов (`EADDRINUSE`).

**Инструкция по Запуску/Проверке Статуса:**

1.  **Проверка и Запуск Компилятора (`tsc --watch`):**
    *   **Проверка:** `pgrep -f "tsc --watch --preserveWatchOutput"` (Если команда что-то вывела, процесс запущен).
    *   **Запуск (если не запущен):**
        ```bash
        pnpm run build:watch &
        ```
2.  **Проверка и Запуск Inngest Dev Server (`dev:serve`):**
    *   **Проверка:** `lsof -i :8288` (Если есть вывод со статусом LISTEN, сервер запущен) ИЛИ `pgrep -f "inngest-cli dev"`.
    *   **Запуск (если не запущен):**
        ```bash
        pnpm run dev:serve &
        ```
3.  **Проверка и Запуск Сервера Приложения (`start`):**
    *   **Проверка:** `lsof -i :8484` (Если есть вывод со статусом LISTEN, сервер запущен).
    *   **Запуск (если не запущен):** Убедись, что сборка (`dist/`) актуальна, и запусти:
        ```bash
        pnpm run start # Лучше в основном потоке для логов
        ```
4.  **Ожидание:** Между запусками (особенно после фоновых) давай системе несколько секунд (5-10) на инициализацию.

**Полный Сброс (Только при Проблемах):**

*   Если возникают ошибки `EADDRINUSE` или другие проблемы, используй полный сброс:
    ```bash
    bash scripts/kill-ports.sh
    pkill -f 'node dist/index.js' && pkill -f 'tsc --watch' && pkill -f 'inngest-cli dev'
    rm -rf dist && pnpm run build
    # После этого снова запускай компоненты по шагам 1-5, проверяя статус.
    ```

**🚫 ЧТО НЕ ДЕЛАТЬ:**

*   **НЕ ИСПОЛЬЗУЙ `pnpm run dev`**: Эта команда нестабильна.
*   **НЕ ЗАПУСКАЙ компоненты без предварительной проверки**, если хочешь избежать лишних перезапусков.

**Цель:** Обеспечить надежный, предсказуемый запуск и работу среды разработки, минимизируя конфликты и перезапуски.

*Ом Шанти. Действуй осознанно, не повторяй лишних действий.* 🙏
