---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Правило: Стабильный Запуск Среды Разработки

**Принцип:** Для стабильной работы и отладки, компоненты среды разработки (компилятор TypeScript, Inngest Dev Server, сервер приложения Node.js) должны запускаться раздельно, избегая нестабильности `concurrently` и `nodemon` с их проблемами гонки портов (`EADDRINUSE`).

**Инструкция по Запуску (Стабильный Способ):**

1.  **Очистка и Сборка:** Убедись, что старые процессы остановлены и порты свободны (используй `pkill`, `bash scripts/kill-ports.sh`). Затем выполни первоначальную сборку:
    ```bash
    bash scripts/kill-ports.sh
    pkill -f 'node dist/index.js' && pkill -f 'tsc --watch' && pkill -f 'inngest-cli dev' # Убедись, что все остановлено
    rm -rf dist && pnpm run build
    ```
2.  **Компиляция в Фоне:** Запусти компилятор TypeScript в режиме наблюдения в фоновом процессе:
    ```bash
    pnpm run build:watch &
    ```
    *(Символ `&` отправляет процесс в фон. Можешь использовать `nohup ... &` для большей надежности или запустить в отдельном окне терминала).*
3.  **Inngest Dev Server в Фоне:** Запусти Inngest Dev Server, указав URL твоего приложения, также в фоне:
    ```bash
    pnpm run dev:serve &
    ```
    *(Аналогично, можно использовать `nohup` или отдельный терминал).*
4.  **Ожидание:** Подожди несколько секунд (5-10), чтобы первые два процесса успели запуститься.
5.  **Сервер Приложения:** Запусти основной сервер приложения **напрямую** через Node.js (НЕ через `nodemon`):
    ```bash
    pnpm run start # Это эквивалентно node dist/index.js
    ```
    *Этот процесс лучше запускать в **основном потоке** (без `&`), чтобы видеть логи приложения и функции Inngest.*

**🚫 ЧТО НЕ ДЕЛАТЬ:**

*   **НЕ ИСПОЛЬЗУЙ `pnpm run dev`**: Эта команда использует `concurrently` и `nodemon`, что приводило к постоянным ошибкам `EADDRINUSE` и нестабильности.
*   **НЕ ИСПОЛЬЗУЙ `pnpm run dev:start`**: Этот скрипт использует `nodemon`, который вызывал проблемы с перезапуском и портами. Используй `pnpm run start` или `node dist/index.js` напрямую.

**Цель:** Обеспечить надежный, предсказуемый запуск всех необходимых компонентов для локальной разработки и тестирования функций Inngest.

*Ом Шанти. Следуй пути стабильности, избегай хаоса одновременности.* 🙏
