---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Правило: Рабочий Процесс Разработки (dev)

**Принцип:** Для гармоничного процесса разработки и постоянного наблюдения за состоянием кода через тесты, используй единую точку входа, которая также освобождает необходимые порты.

**Инструкция:**

*   **ВСЕГДА используй `pnpm run dev`** для запуска локального окружения разработки.
    *   Эта команда, определенная в [`package.json`](mdc:package.json):
        1.  Запускает [`scripts/kill-ports.sh`](mdc:scripts/kill-ports.sh) для освобождения портов 8288, 8289, 4173.
        2.  Использует `concurrently` для одновременного запуска с цветными логами:
            *   `pnpm run dev:serve` (`npx inngest-cli dev`) - Основной сервер Inngest.
            *   `pnpm run dev:test-report` (`npx vite preview --outDir html`) - Сервер для просмотра HTML-отчета тестов (обычно доступен на `http://localhost:4173`).
*   **НЕ ЗАПУСКАЙ `pnpm run dev:serve` или `pnpm run dev:test-report` отдельно** в процессе обычной разработки, так как это не обеспечит полного окружения и очистки портов.
*   **Цель:** Обеспечить надежный запуск полного окружения разработки одной командой, включая доступ к HTML-отчету тестов.

*Ом Шанти. Устраняй препятствия перед началом пути.* 🙏
