---
description: 
globs: 
alwaysApply: false
---
# 🕉️ Правило: Запуск и Мониторинг Тестов (Vitest)

**Принцип:** Для эффективной разработки и немедленной обратной связи используй интерактивный интерфейс Vitest UI. Статический HTML-отчет предназначен для итоговых результатов или CI.

**Инструкции:**

1.  **Интерактивный UI (Рекомендуется для Разработки):**
    *   **Запуск:** Используй команду `pnpm run test:ui`.
        *   Этот скрипт (из [`package.json`](mdc:package.json)) запускает `vitest --ui`.
        *   Тесты будут выполняться в режиме наблюдения (`--watch` по умолчанию с `--ui`) и автоматически перезапускаться при изменении файлов.
    *   **Просмотр:** Открой в браузере адрес, указанный в консоли после запуска (обычно `http://localhost:51204/__vitest__/`). Ты увидишь результаты тестов в реальном времени.
    *   **С Покрытием:** Если хочешь видеть и отчет о покрытии в интерактивном UI, используй `pnpm run test:watch`.
    *   **Документация:** [Vitest UI Guide](mdc:https:/vitest.dev/guide/ui.html#vitest-ui)

2.  **Статический HTML-Отчет (Опционально, для CI или Финального Прогона):**
    *   **Генерация:**
        *   Добавь `'html'` в массив `reporters` в файле [`vitest.config.ts`](mdc:vitest.config.ts).
        *   Запусти тесты командой `pnpm test` (или `vitest run --reporter=html`). Отчет будет сгенерирован в директории `html/` (или как указано в опции `outputFile`).
    *   **Просмотр:** Используй команду `pnpm run dev:test-report` (`npx vite preview --outDir html`). Эта команда *не генерирует* отчет, а лишь запускает сервер для просмотра *уже существующего* файла `html/index.html`.

**🚫 ЧТО НЕ ДЕЛАТЬ (для онлайн просмотра):**

*   **Не полагайся на `pnpm run dev:test-report` для просмотра тестов в реальном времени.** Он показывает только статический, ранее сгенерированный отчет.

**Цель:** Обеспечить ясное понимание, как использовать инструменты Vitest для эффективного мониторинга тестов во время разработки.

*Ом Шанти. Наблюдай за потоком тестов в реальном времени, обретая ясность.* 🙏
