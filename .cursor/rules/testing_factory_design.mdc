---
description: 
globs: 
alwaysApply: true
---
# 🕉️ Дизайн Фабрики для Тестирования Функций Inngest

**Принцип:** Чистый, предсказуемый и переиспользуемый тестовый стенд – основа для стабильного TDD-цикла.

**Цель:** Создать документированную фабрику (`TestFactory`) или набор утилит в `src/__tests_focused__/`, которая будет предоставлять стандартизированные моки и зависимости для unit- и интеграционных тестов функций Inngest и агентов AgentKit. Это упростит написание тестов, повысит их читаемость и поддерживаемость, а также станет основой для возможной будущей библиотеки.

**Этот документ будет обновляться шаг за шагом по мере разработки фабрики.**

🧘‍♂️ ФОКУС ТОЛЬКО НА TEAMLEAD И ФАБРИКЕ ТЕСТОВ! 🧘‍♂️
🧘‍♂️ ДРУГИХ АГЕНТОВ НЕ ТРОГАЕМ! 🧘‍♂️

Проверка Типов: Запустить bun exec tsc --noEmit.
Запуск Теста (Только после успеха TS): Если проверка типов пройдет, запустить bun vitest run src/__tests_focused__/teamLeadAgentNetwork.REAL.integration.test.ts --isolate 

---

Понял тебя, Гуру. Возвращаемся к стратегическому видению. Изучил результаты поиска и документацию, которую ты предоставил.

**Ключевые моменты из документации:**

*   **`@inngest/test`:** Позволяет нам использовать `InngestTestEngine` для тестирования функций Inngest. Мы можем мокировать конкретные шаги (`step.run`, `step.sleep`, `step.waitForEvent` и т.д.) через опцию `steps` в `t.execute()`. Это отлично подходит для unit- и интеграционных тестов *отдельных* функций Inngest, изолируя их от реальной логики шагов. (`minimalExample.test.ts` уже использует этот подход).
*   **AgentKit:**
    *   Предлагает концепции **Агентов**, **Инструментов**, **Сетей (Networks)**, **Состояния (State)** и **Маршрутизаторов (Routers)**.
    *   **Агенты** могут использовать **Инструменты**, которые могут взаимодействовать с внешним миром или изменять **Состояние** Сети.
    *   **Сети** координируют работу нескольких Агентов с помощью **Маршрутизатора** (либо кодового, либо другого Агента) и общего **Состояния**.
    *   Тестирование здесь усложняется: нам нужно тестировать не только *одного* агента, но и их *взаимодействие* в Сети, работу Маршрутизатора и корректное изменение Состояния.
*   **Детерминированное Тестирование:** AgentKit подчеркивает важность *детерминированного* тестирования через управление состоянием. Это значит, что наша фабрика должна позволять легко устанавливать начальное состояние сети и проверять конечное состояние.

**Выводы для нашей Фабрики Тестирования:**

1.  **Два Уровня Тестирования:** Нам понадобятся утилиты для двух основных сценариев:
    *   **Unit-тесты Функций/Агентов:** Тестирование логики *одного* агента или *одной* функции Inngest в изоляции. Здесь хорошо подходит `@inngest/test` с мокированием шагов и базовая фабрика моков (логгер, зависимости).
    *   **Интеграционные/Сетевые Тесты:** Тестирование *взаимодействия* агентов внутри Сети AgentKit, включая работу маршрутизатора и изменение состояния. Здесь потребуется более сложный setup: возможность передавать реальные (или специально созданные тестовые) агенты в Сеть, устанавливать начальное состояние `network.state.kv` и проверять его после выполнения `network.run()`.
2.  **Мокирование Инструментов:** Агенты используют Инструменты. Нам нужен способ легко мокировать Инструменты (`Tool<any>`), чтобы контролировать их поведение в тестах (например, чтобы `readFile` возвращал предопределенный контент или `updateTaskState` проверял правильность переданных данных).
3.  **Управление Состоянием Сети:** Фабрика должна предоставлять утилиты для легкого создания начального состояния `TddNetworkState` для тестов Сети и для проверки его полей после завершения работы Сети.

**Обновленный Roadmap для `@testing_factory_design.mdc`:**

(Вместо того чтобы редактировать файл напрямую, я сначала предложу здесь обновленный план, чтобы мы могли его обсудить и утвердить)

---

**Предлагаемый Обновленный План в `@testing_factory_design.mdc`:**

## 📝 Текущие Шаги / План (Детализированный)

**I. Основа Фабрики (Unit-тесты Функций/Агентов)**

1.  ✅ **Создать этот файл** (`testing_factory_design.mdc`).
2.  ✅ **Определить Базовые Моки:**
    *   Файл `src/__tests_focused__/testSetupFocused.ts`.
    *   Моки: `mockLogger`, `mockSystemEvents`, константы (`mockApiKey`, `mockModelName`, `mockEventId`).
    *   Функция `setupTestEnvironmentFocused()` (`mock.resetAllMocks()`).
    *   Функция `createBaseMockDependencies()` (без инструментов/агентов).
    *   Мок для `deepseek` модели.
3.  ✅ **Интегрировать Базовые Моки (`@inngest/test`):**
    *   Адаптировать `minimalExample.test.ts` для использования `setupTestEnvironmentFocused` и `createBaseMockDependencies`.
    *   Убедиться, что он проходит (демонстрирует использование `@inngest/test` с моком шага).
4.  ✅ **Фабрика для Агентов:**
    *   Определить интерфейс/функцию для создания мок-агентов (`createMockAgent`).
    *   Добавить создание мок-агента TeamLead в `testSetupFocused.ts`.
5.  ✅ **Адаптация Теста TeamLead:**
    *   Скопировать `teamLeadAgent.test.ts` в `src/__tests_focused__/teamLeadAgent.unit.test.ts`.
    *   Адаптировать его для использования `testSetupFocused.ts`.
    *   Убедиться в прохождении базовых тестов (создание агента, проверка имени/описания).
6.  ✅ **Фабрика Мок-Инструментов:**
    *   Пересмотреть `mockTools` в `testSetupFocused.ts`. Сделать их более функциональными моками (`mock()`), а не просто объектами.
    *   Создать хелпер `findToolMock(name)` для легкого доступа к моку инструмента по имени (чтобы проверять вызовы `mockTool.handler`).
    *   Создать функцию `getMockTools(names: string[])` для получения только нужного набора мок-инструментов для конкретного теста.

**II. Адаптация Unit-теста TeamLead (Используя Фабрику)**

7.  ⏸️ **Подготовка к Сетевым Тестам:** (Пропущено)
    *   Создать функцию `createMockNetworkState(initialData?: Partial<TddNetworkState>)` для генерации начального состояния сети.
    *   Определить, как будем тестировать Сеть (`createDevOpsNetwork`):
        *   Вариант А: Передавать *реальные* агенты (`createTeamLeadAgent`, `createCriticAgent` и т.д.), но с *мокированными* Инструментами.
        *   Вариант Б: Передавать *мокированные* агенты (`mockTeamLeadAgent` и т.д.) и мокировать их ответы (`mockAgent.ask.mockResolvedValue(...)`).
        *   *Предпочтение:* Начать с Варианта А, так как он ближе к реальности, но требует хорошего мокирования инструментов.
8.  ➡️ **Интеграционный Тест TeamLead (в Сети):**
    *   **Задача:** Создать `src/__tests_focused__/teamLeadNetwork.integration.test.ts`.
    *   **Цель:** Проверить, что TeamLead агент, будучи запущенным через `network.run()`, корректно вызывает инструмент `updateTaskState` после генерации требований.
    *   Использовать `createDevOpsNetwork` с *реальным* `createTeamLeadAgent` и *моками* остальных агентов (или пока без них, если роутер позволяет).
    *   Передать *мокированные* инструменты (`getMockTools(['updateTaskState', 'web_search'])`).
    *   Установить начальное состояние через `createMockNetworkState`.
    *   Запустить `await network.run("Initial task description")`.
    *   Проверить, что мок `findToolMock('updateTaskState').handler` был вызван с ожидаемыми параметрами.
    *   Проверить финальное состояние сети (например, что `status` стал `NEEDS_REQUIREMENTS_CRITIQUE`).

**III. Расширение и Рефакторинг**

9.  🟡 **Тесты для Других Агентов (Unit):**
    *   **Статус:** Частично выполнены (`criticAgent.unit.test.ts`, `coderAgent.unit.test.ts`, `testerAgent.unit.test.ts` созданы). ⏸️ **Отложены до завершения фокуса на TeamLead.**
    *   Адаптировать/создать unit-тесты для `createCriticAgent`, `createCoderAgent` и т.д., проверяя их создание и фильтрацию инструментов, используя фабрику.

10. ❌ **Тесты для Других Взаимодействий (Интеграционные - ЗАБЛОКИРОВАНО):**
    *   **Статус:** ЗАБЛОКИРОВАНО из-за внутренней ошибки `TypeError: Cannot read properties of undefined (reading 'request')` в `agent-kit` при вызове `network.run`.
    *   **План:** Отложить интеграционное тестирование сети до исправления ошибки в `agent-kit`. Фокус на unit-тестах агентов и тестировании Inngest-функций с мокированием шага `step.run('run-agent-network', ...)`.
    *   Создать тесты для проверки других шагов TDD цикла в Сети (например, Critic вызывает рефакторинг, Coder пишет код и т.д.).

11. ⏸️ **Рефакторинг и Документация Фабрики:** (Отложено)
    *   Улучшить структуру `testSetupFocused.ts`.
    *   Добавить JSDoc комментарии.
    *   Обновить этот документ финальными решениями.

**IV. Завершение**

12. ⏸️ **Очистка:** (Отложено)
    *   Удалить `src/__tests_backup__`.
    *   Заменить содержимое `src/__tests__` на `src/__tests_focused__`.
    *   Раскомментировать инструмент в `src/tools/toolDefinitions.ts`.
    *   Проверить все тесты (`bun run test`).

---

**Гуру, как тебе такой детализированный план?** Он учитывает разные уровни тестирования (unit, интеграция), необходимость мокирования инструментов и управления состоянием сети. Если план устраивает, я внесу его в `@testing_factory_design.mdc` и мы перейдем к **Шагу 7: Подготовка к Сетевым Тестам**.

## 📜 История Решений / Лог

*   **2025-04-27:** Принято решение остановить попытки исправить текущие тесты из-за высокой сложности и запутанности. Создан новый подход: бэкап старых тестов, создание новой директории `src/__tests_focused__`, запуск базового рабочего теста (`minimalExample.test.ts`) в изоляции (после временного комментирования проблемного импорта в `toolDefinitions.ts`). Создан этот документ для пошаговой разработки фабрики тестирования.
*   **2025-04-27:** Шаг 2 (Базовые Моки) завершен: Создан `testSetupFocused.ts` с `mockLogger`, `mockSystemEvents`, константами, `setupTestEnvironmentFocused` и `createBaseMockDependencies`.
*   **2025-04-27:** Шаг 3 (Интеграция Базовых Моков) завершен: `minimalExample.test.ts` проверен, он уже использует `setupTestEnvironmentFocused` и успешно проходит в изоляции.
*   **2025-04-27:** Шаг 4 (Фабрика для Агентов) завершен: Добавлен `mockTeamLeadAgent` в `testSetupFocused.ts`.
*   **2025-04-28:** Шаг 5 (Адаптация Теста TeamLead) завершен: `teamLeadAgent.unit.test.ts` скопирован, адаптирован под новую фабрику и успешно проходит базовые тесты.
*   **2025-04-28:** Шаг 6 (Фабрика Мок-Инструментов) завершен: Убедился, что хендлеры - `mock()`. Добавлены хелперы `findToolMock` и `getMockTools` в `testSetupFocused.ts`.
*   **2025-04-28:** Шаг 9 (Unit-тесты других агентов) - **Частично выполнены преждевременно:** Созданы и прошли `criticAgent.unit.test.ts`, `coderAgent.unit.test.ts`. Создан `testerAgent.unit.test.ts` (но не запущен). **Фокус возвращен на Шаг 8.**
*   **2025-04-28:** Шаг 8 (Интеграционный тест TeamLead) - **ЗАБЛОКИРОВАНО:** Многократные попытки запустить `network.run` в тесте `teamLeadNetwork.integration.test.ts` провалились с ошибкой `TypeError: Cannot read properties of undefined (reading 'request')` в `agent-kit/src/model.ts:17`. Проблема сохраняется при использовании реальных/моковых моделей, реальных/моковых агентов (с `mock.spyOn`). Вероятно, внутренняя ошибка `agent-kit`.

🕉️ Махабхарата, Удьйогапарва (5.31.25):
"योजनानां सहस्रं तु शनैर्गच्छेत् पिपीलिका । आगच्छन् वैनतेयोऽपि पदमेकं न गच्छति ॥"
(yojanānāṃ sahasraṃ tu śanairgacchet pipīlikā | āgacchan vainateyo'pi padamekaṃ na gacchati ||)
*Перевод:* "Муравей, двигаясь медленно, может пройти тысячу йоджан. Но Гаруда, даже взлетев, не сдвинется и на шаг, если у него нет цели."
*Мудрость:* Ты абсолютно прав, Гуру. Без четкого, детализированного плана, даже самые быстрые действия могут оказаться бесцельными. Потратим время на планирование сейчас, чтобы потом двигаться уверенно и быстро, как муравей к своей цели.

Погружаюсь в священные тексты документации Inngest и AgentKit, чтобы осветить наш путь и составить подробную карту (Roadmap) для нашей Фабрики Тестирования.

Проведу веб-поиск, чтобы убедиться, что у меня самая свежая информация по тестированию с `@inngest/test` и AgentKit.
