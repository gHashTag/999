---
description: 
globs: 
alwaysApply: false
---
# ⚡ Правило: Запуск Тестовых Событий Inngest

**Принцип:** Для обеспечения консистентности, надежности и автономности при запуске тестовых событий Inngest во время разработки, используется стандартизированный скрипт.

**Инструкция:**

*   **ВСЕГДА используй скрипт [`scripts/send-test-event.mjs`](mdc:scripts/send-test-event.mjs)** для отправки тестовых событий `coding-agent/run` (или других событий Inngest) через терминальный инструмент.
    *   Пример вызова (использует тестовые данные по умолчанию):
        ```bash
        node scripts/send-test-event.mjs
        ```
    *   Пример вызова с кастомной строкой `input`:
        ```bash
        node scripts/send-test-event.mjs "Ваш кастомный промпт здесь"
        ```
    *   Пример вызова с кастомным JSON `data` (передавать как строку JSON):
        ```bash
        node scripts/send-test-event.mjs '{"input": "Другой промпт", "someOtherKey": 123}'
        ```
*   **Убедись, что `pnpm run dev` запущен**, так как скрипт отправляет запрос на `localhost:8288`.
*   **НЕ ИСПОЛЬЗУЙ `curl`** напрямую для отправки событий, так как он показал себя ненадежным в терминальном инструменте.
*   **НЕ ПРОСИ Пользователя (Гуру)** запускать события вручную через UI или `curl`.

**Цель:** Стандартизировать запуск тестовых событий, повысить автономность агента, избежать проблем с ненадежными командами.

*Ом Шанти. Пусть действия твои будут точны и предсказуемы.* 🙏
