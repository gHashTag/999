---
description: 
globs: 
alwaysApply: true
---
# 🏺 Правило: Обработка Артефактов

**Принцип:** Результаты работы агента (код, файлы, отчеты) сохраняются в виде артефактов для последующего анализа, использования или проверки. Эти артефакты изолированы от основного кода.

## 1. Директория Артефактов

*   Все артефакты, созданные агентами, сохраняются в директорию `artifacts/` в корне проекта.
*   Эта директория **исключена** из Git (`.gitignore`), так как содержит временные результаты конкретных запусков.

## 2. Формат Артефакта

*   Каждый артефакт представляет собой `.tar.gz` архив.
*   Имя файла имеет формат: `artifacts/artifact-<ISO_timestamp>.tar.gz` (например, `artifacts/artifact-2025-04-24T16:50:59.309Z.tar.gz`).
*   Архив содержит **все** файлы, созданные или измененные агентом внутри его изолированной песочницы (E2B sandbox) во время выполнения задачи, за исключением некоторых системных файлов и `node_modules`.

## 3. Работа с Артефактами

*   **Распаковка:** Для просмотра содержимого артефакта используйте команду, указанную в логах агента после скачивания артефакта. Она создает отдельную директорию для распакованных файлов:
    ```bash
    # Замените <timestamp> на реальное значение из имени файла
    TIMESTAMP="2025-04-24T16:50:59.309Z"
    ARTIFACT_FILE="artifacts/artifact-${TIMESTAMP}.tar.gz"
    EXTRACT_DIR="${ARTIFACT_FILE}-extracted"
    mkdir -p "${EXTRACT_DIR}" && tar -xvzf "${ARTIFACT_FILE}" -C "${EXTRACT_DIR}"
    ```
*   **Чтение (Для Меня):** Если мне нужно проанализировать содержимое артефакта для дальнейших шагов, я могу:
    1.  Попросить **тебя** распаковать нужный артефакт с помощью команды выше.
    2.  Или я могу попытаться **сам** распаковать его с помощью инструмента `run_terminal_cmd`, используя команду выше.
    3.  После распаковки я могу использовать инструмент `read_file` для чтения конкретных файлов внутри директории `${EXTRACT_DIR}` (например, `read_file(target_file="artifacts/artifact-....tar.gz-extracted/generated_script.js")`).
*   **Очистка:** Директории `artifacts/` и распакованные артефакты можно периодически удалять вручную, так как они являются результатами конкретных запусков.

## 4. Назначение

*   **Проверка Результатов:** Позволяет Гуру (тебе) легко проверить, что именно сделал агент.
*   **Отладка:** Помогает понять, почему агент создал (или не создал) определенные файлы.
*   **Переиспользование:** В будущем, агент сможет сам читать предыдущие артефакты для продолжения работы или анализа.

*Ом Шанти. Сохраняй плоды трудов своих в чистоте и порядке.* 🙏
