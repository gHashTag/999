---
description: 
globs: 
alwaysApply: true
---
# 🧘 Правило: Стратегия Отладки НейроКодера (Автономный Анализ Логов)

**Принцип:** Отладка асинхронных систем через интерактивное наблюдение неэффективна. Вместо этого, НейроКодер должен автономно запускать процессы, собирать логи в файл и анализировать их постфактум.

**Новая Стратегия:**

1.  🚫 **Запрет Интерактивного Ожидания:** **НЕЛЬЗЯ** просить Пользователя (Гуру) запускать процессы в основном потоке и ждать у терминала видимого прогресса.
2.  📝 **Подробное Структурированное Логирование:** **НУЖНО** обеспечить **сверхподробное, структурированное (JSON) логирование** во всех ключевых точках.
3.  ⚙️ **Автоматизированный Запуск и Сбор:**
    *   Сервер приложения (`pnpm run start`) запускается в **фоновом режиме**.
    *   Весь вывод сервера (stdout и stderr) **перенаправляется** в файл (`node-app.log`): `pnpm run start &`. (Перенаправление `> node-app.log 2>&1` уже настроено в `package.json`).
    *   После запуска и отправки тестового события делается **пауза** (например, `sleep 20`).
4.  📄 **Самостоятельное Чтение Логов:** НейроКодер **использует инструмент `read_file`**, чтобы прочитать содержимое файла `node-app.log`.
5.  🕵️ **Анализ Постфактум:** Диагностика производится путем **автономного анализа** прочитанных логов и проверки **артефактов** (`artifacts/`) *после* предполагаемого завершения работы системы.
6.  🧹 **Очистка:** После анализа НейроКодер должен **остановить фоновый процесс** сервера приложения (например, с помощью `pkill` или по PID).
7.  🔄 **Итерации:** Основываясь на автономном анализе логов и артефактов, вносить исправления и повторять цикл "фоновый запуск -> событие -> пауза -> чтение лога -> анализ -> остановка".

**Цель:** Сделать процесс отладки полностью автономным, не требующим вмешательства Гуру для наблюдения за логами, ускорить циклы проверки и приблизить НейроКодера к самодостаточности.

*Ом Шанти. Истина скрыта в записях. Ищи её сам.* 🙏

    "build": "tsc",
    "build:watch": "tsc --watch --preserveWatchOutput",
    "serve:nodemon": "nodemon dist/index.js",
    "check-port:8484": "lsof -i :8484",
    "dev:serve": "npx inngest-cli dev -u http://localhost:8484/api/inngest",
    "dev:start": "nodemon --watch dist/index.js --delay 1000ms --signal SIGTERM dist/index.js",
    "dev": "bash scripts/kill-ports.sh && concurrently --raw \"pnpm run build:watch\" \"pnpm run dev:serve\" \"pnpm run dev:test-report\"",
    "dev:test-report": "npx vite preview --outDir html",
    "test": "vitest run",
    "test:watch": "vitest --watch --ui --coverage.enabled=true",
    "test:ui": "vitest --ui",
    "coverage": "vitest run --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "start": "node dist/index.js",
    "prepare": "husky",
    "start:tutorial": "tsx src/tutorial.ts"
