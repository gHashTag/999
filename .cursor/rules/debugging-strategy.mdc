---
description: 
globs: 
alwaysApply: true
---
# 🧘 Правило: Стратегия Отладки НейроКодера (Избегание Интерактивного Ожидания)

**Принцип:** Отладка асинхронных и распределенных систем (как Inngest с агентами) путем интерактивного наблюдения за процессами в реальном времени оказалась неэффективной и вызывающей фрустрацию в данном проекте. Часто процессы кажутся "зависшими", ошибки не видны сразу, а фоновый режим скрывает важную информацию.

**Новая Стратегия:**

1.  🚫 **Запрет Интерактивного Ожидания:** **НЕЛЬЗЯ** просить Пользователя (Гуру) запускать процессы (`node`, `concurrently`) в основном потоке и ждать у терминала видимого прогресса или логов в реальном времени для отладки зависаний или медленной работы.
2.  📝 **Приоритет Подробного Логирования:** Вместо этого, **НУЖНО** обеспечить **сверхподробное, структурированное (JSON) логирование** во всех ключевых точках кода (обработчики, инструменты, хуки жизненного цикла, обработка ошибок, обновление состояния). Логи должны содержать максимум контекста (временные метки, ID событий, имена шагов, значения переменных).
3.  🕵️ **Анализ Постфактум:** Диагностика проблем должна производиться путем **анализа собранных логов** (`node-app.log`, `inngest-cli.log` и т.д.) и проверки **созданных артефактов** (`artifacts/`) *после* того, как система отработала (или не отработала) в течение разумного времени (например, 1-2 минуты после отправки события).
4.  🏺 **Проверка Артефактов:** Всегда проверять наличие и содержимое артефактов в `artifacts/` как часть диагностики.
5.  🔄 **Итерации:** Основываясь на анализе логов и артефактов, вносить исправления и повторять цикл "запуск -> ожидание -> сбор логов/артефактов -> анализ".

**Цель:** Сделать процесс отладки менее зависимым от непредсказуемого поведения процессов в реальном времени, более надежным и менее фрустрирующим для Гуру. Перейти от "наблюдения за медитацией" к "чтению священных текстов логов".

    "build": "tsc",
    "build:watch": "tsc --watch --preserveWatchOutput",
    "serve:nodemon": "nodemon dist/index.js",
    "check-port:8484": "lsof -i :8484",
    "dev:serve": "npx inngest-cli dev -u http://localhost:8484/api/inngest",
    "dev:start": "nodemon --watch dist/index.js --delay 1000ms --signal SIGTERM dist/index.js",
    "dev": "bash scripts/kill-ports.sh && concurrently --raw \"pnpm run build:watch\" \"pnpm run dev:serve\" \"pnpm run dev:test-report\"",
    "dev:test-report": "npx vite preview --outDir html",
    "test": "vitest run",
    "test:watch": "vitest --watch --ui --coverage.enabled=true",
    "test:ui": "vitest --ui",
    "coverage": "vitest run --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "start": "node dist/index.js",
    "prepare": "husky",
    "start:tutorial": "tsx src/tutorial.ts"

*Ом Шанти. Истина скрыта в деталях, ищите ее в записях.* 🙏
